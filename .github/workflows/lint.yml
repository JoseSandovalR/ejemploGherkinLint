name: E2E Test

on: [push]

jobs:
  Gherkin-lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: JoseSandovalR/gp-linter-actions@1.0
      with:
        path: 'src/test/resources/feature'
        config_file: './.gherkin-lintrc'
